name: CI

on: [push, pull_request]

env:
  INVALID_VAR: ${{ secrets.NON_EXISTENT_SECRET
  BROKEN: {{ missing_context }}

jobs:
  build:
    runs-on: windows-nonexistent-2099
    needs: non-existent-job
    if: ${{ github.event_name == 'push' && 
  invalid-indentation:
      this: is: broken:

    steps:
    - uses: actions/checkout@v999
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm ci
    - run: npm run lint
    - run: npm run test
    - name: Invalid Step
      run: |
        echo "test"
      invalid_key: "this will cause an error"
